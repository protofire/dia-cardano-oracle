# Aiken Contracts

This package contains the first on-chain implementation for the DIA Cardano Oracle project.

## Structure

- `validators/` contains spending and minting validators.
- `lib/` contains shared types, validation helpers, and unit tests.

## Commands

```sh
aiken check
aiken build
```
